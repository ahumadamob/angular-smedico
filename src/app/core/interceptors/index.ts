import { Provider } from '@angular/core';

/**
 * Placeholder collection for HTTP interceptors that will be registered via DI.
 * Future interceptors should push their provider definitions into this array
 * so they are automatically included when the application bootstraps.
 */
export const HTTP_INTERCEPTOR_PROVIDERS: Provider[] = [];
