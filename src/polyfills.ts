/**
 * Angular's change detection relies on Zone.js.
 * Import the UMD bundle explicitly so Vite can resolve it during dev builds
 * even when the package exports field is interpreted strictly.
 */
import 'zone.js/dist/zone';
